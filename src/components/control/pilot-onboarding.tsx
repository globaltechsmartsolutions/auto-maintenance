"use client";

import * as React from "react";
import Link from "next/link";
import type { Route } from "next";
import { CheckCircle2, Circle, FileUp, Rocket } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

type Progress = { customers: number; worksites: number; employees: number; services: number; shifts: number; clockEvents: number };
const steps: Array<{ key: keyof Progress; label: string; description: string; href: Route }> = [
  { key: "customers", label: "Add a customer", description: "Identify the client receiving the service.", href: "/crm" },
  { key: "worksites", label: "Create a worksite", description: "Set the location and verification method.", href: "/worksites" },
  { key: "employees", label: "Invite the field team", description: "Add skills, zones, and availability.", href: "/employees" },
  { key: "services", label: "Create a client service", description: "Record the operational commitment.", href: "/services" },
  { key: "shifts", label: "Plan the first shift", description: "Link the worksite, service, and worker.", href: "/shifts" },
  { key: "clockEvents", label: "Complete the first clock", description: "Verify the end-to-end attendance flow.", href: "/employee" },
];

export function PilotOnboarding() {
  const [progress, setProgress] = React.useState<Progress>();
  const [importKind, setImportKind] = React.useState("WORKSITES");
  const [preview, setPreview] = React.useState<{ rowCount: number; validRows: number; invalidRows: number; issues: Array<{ row: number; field: string; message: string }> }>();
  const [previewError, setPreviewError] = React.useState<string>();
  const [csv, setCsv] = React.useState<string>();
  const [confirmationStatus, setConfirmationStatus] = React.useState<"idle" | "confirming" | "done">("idle");
  const [confirmationResults, setConfirmationResults] = React.useState<Array<{ row: number; status: string; message: string }>>();
  React.useEffect(() => { void fetch("/api/control/onboarding", { cache: "no-store" }).then(async (response) => response.ok ? response.json() : undefined).then((body) => { if (body?.progress) setProgress(body.progress); }).catch(() => undefined); }, []);
  const complete = steps.filter((step) => (progress?.[step.key] ?? 0) > 0).length;
  async function previewFile(file?: File) { if (!file) return; setPreviewError(undefined); setPreview(undefined); setConfirmationResults(undefined); try { const contents = await file.text(); setCsv(contents); const response = await fetch("/api/control/imports/preview", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ kind: importKind, csv: contents }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "The CSV could not be previewed."); setPreview(body.preview); } catch (error) { setPreviewError(error instanceof Error ? error.message : "The CSV could not be previewed."); } }
  async function confirmImport() { if (!csv || !preview || preview.invalidRows || importKind === "EMPLOYEES") return; setConfirmationStatus("confirming"); setPreviewError(undefined); try { const response = await fetch("/api/control/imports/confirm", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ kind: importKind, csv }) }); const body = await response.json(); if (!response.ok) throw new Error(body.error ?? "The CSV could not be imported."); setConfirmationResults(body.results ?? []); setConfirmationStatus("done"); void fetch("/api/control/onboarding", { cache: "no-store" }).then(async (next) => next.ok ? next.json() : undefined).then((next) => { if (next?.progress) setProgress(next.progress); }); } catch (error) { setPreviewError(error instanceof Error ? error.message : "The CSV could not be imported."); setConfirmationStatus("idle"); } }
  return <div className="space-y-6"><div><p className="text-sm text-muted-foreground">Controlled pilot</p><h1 className="mt-1 text-3xl font-semibold">Pilot setup</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Complete one verifiable service cycle before inviting a customer to the pilot.</p></div><Card className="border-primary/25 bg-card/90 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2"><Rocket className="size-5 text-primary" />First operational cycle</CardTitle><CardDescription>{progress ? `${complete} of ${steps.length} steps complete` : "Loading workspace progress…"}</CardDescription></CardHeader><CardContent className="space-y-3">{steps.map((step, index) => { const done = (progress?.[step.key] ?? 0) > 0; return <Link href={step.href} key={step.key} className="flex items-center justify-between gap-4 rounded-lg border border-border/70 p-4 transition hover:bg-muted/40"><div className="flex items-start gap-3">{done ? <CheckCircle2 className="mt-0.5 size-5 text-success" /> : <Circle className="mt-0.5 size-5 text-muted-foreground" />}<div><p className="font-medium">{index + 1}. {step.label}</p><p className="mt-1 text-sm text-muted-foreground">{step.description}</p></div></div><Badge variant={done ? "secondary" : "outline"}>{done ? "Complete" : "Open"}</Badge></Link>; })}</CardContent></Card><Card className="border-border/70 bg-card/90 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2"><FileUp className="size-5 text-primary" />CSV import</CardTitle><CardDescription>Preview every row first. Confirmation creates all valid non-duplicate rows in one transaction.</CardDescription></CardHeader><CardContent className="space-y-4"><div className="flex flex-col gap-3 sm:flex-row"><select value={importKind} onChange={(event) => { setImportKind(event.target.value); setPreview(undefined); setCsv(undefined); setConfirmationResults(undefined); }} className="h-8 rounded-lg border border-input bg-background px-2.5 text-sm"><option value="EMPLOYEES">Employees</option><option value="WORKSITES">Worksites</option><option value="SERVICES">Services</option><option value="SHIFTS">Shifts</option></select><input aria-label="CSV file" type="file" accept=".csv,text/csv" onChange={(event) => void previewFile(event.target.files?.[0])} className="block text-sm" /></div>{previewError ? <p className="text-sm text-destructive">{previewError}</p> : null}{preview ? <div className="rounded-lg border border-border/70 p-3 text-sm"><p className="font-medium">{preview.validRows} valid of {preview.rowCount} rows</p><p className="mt-1 text-muted-foreground">{preview.invalidRows} rows need correction. No data has been created.</p>{preview.issues.length ? <div className="mt-3 space-y-1 text-xs text-destructive">{preview.issues.slice(0, 8).map((issue, index) => <p key={`${issue.row}-${issue.field}-${index}`}>Row {issue.row}, {issue.field}: {issue.message}</p>)}</div> : importKind === "EMPLOYEES" ? <p className="mt-3 text-sm text-muted-foreground">Employee files must use the invitation workflow so each person receives a secure account invitation.</p> : <Button type="button" className="mt-3" onClick={() => void confirmImport()} disabled={confirmationStatus === "confirming"}>{confirmationStatus === "confirming" ? "Importing…" : "Confirm import"}</Button>}{confirmationResults ? <div className="mt-3 space-y-1 text-xs">{confirmationResults.map((result) => <p key={result.row} className={result.status === "FAILED" ? "text-destructive" : "text-muted-foreground"}>Row {result.row}: {result.status.toLowerCase()} — {result.message}</p>)}</div> : null}</div> : null}</CardContent></Card></div>;
}
